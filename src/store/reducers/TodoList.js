const INNITAL_STATE = [
  {
    id: 1,
    text: "teste davi",
    context: "reduxx"
  }
];

export default function todos(state = INNITAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
